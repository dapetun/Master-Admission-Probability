# Данные

- `raw/` — CSV конкурсных списков, выгруженные вручную (Госуслуги / личный кабинет).
- Файлы `*.csv` **не коммитятся** в git (см. `.gitignore`).

Формат имени файла (как в выгрузке):

```text
<Название_программы>.YYYY-MM-DD_HH-MM-SS.csv
```
